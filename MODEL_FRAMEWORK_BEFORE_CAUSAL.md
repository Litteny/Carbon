# OpenCarbon 主模型结构

> 本文总结因果扩展前的 `opencarbon_monthly` 主模型，不包含 V-REx、环境风险方差惩罚、预计算 POI 表征等后续模块。

## 1. 模型目标

模型以“城市—网格—月份”为一个样本，融合 POI、遥感、气象和月份信息，预测网格月度碳排放的对数值：

$$
y = \log(1 + \mathrm{emission}_{tc})
$$

整体结构如下：

```text
17 通道 POI 图 ── POI CNN + SE ─────┐
MODIS / VIIRS ─── 遥感 MLP ─────────┼── 模态注意力融合
气象 / 月份 ───── 环境 MLP ─────────┘
                                         │
                                  3×3 邻域聚合
                                ┌────────┴────────┐
                                │                 │
                           空间卷积          多头注意力
                                └────────┬────────┘
                                         │
                              中心表征 + 邻域表征
                                         │
                                      MLP 回归
                                         │
                                log1p 碳排放预测
```

## 2. 输入模态

模型包含三路输入：

1. **POI 空间图**：17 类 POI 构成 $17\times256\times256$ 的栅格图，保留网格内部的设施分布。
2. **遥感特征**：包括 MODIS 植被指数、反射率、有效像元数以及 VIIRS 夜间灯光统计。
3. **环境与时间特征**：包括温度、湿度、降水、风速、短波辐射和 12 维月份 one-hot 编码。

遥感和气象变量的缺失情况通过显式缺失标记表示。连续特征使用训练集的中位数填补并进行标准化。

## 3. 模态编码

### 3.1 POI 编码器

POI 编码器使用三层卷积提取空间特征：

```text
17×256×256 POI 图
    ↓
7×7 Conv + BatchNorm + ReLU + SE
    ↓
7×7 Conv + BatchNorm + ReLU + SE
    ↓
3×3 Conv + Flatten
    ↓
Linear → D 维 POI 表征
```

SE 模块通过通道注意力突出更重要的 POI 类型。默认表征维度为 $D=128$。

### 3.2 遥感与环境编码器

遥感特征和环境/月份特征分别经过独立的两层 MLP，并映射到相同的 $D$ 维空间：

$$
z_{poi},\;z_{remote},\;z_{env}\in\mathbb{R}^{D}
$$

## 4. 模态注意力融合

模型为 POI、遥感和环境三种表征分别学习注意力权重，再进行加权求和：

$$
z_{grid}=a_{poi}z_{poi}+a_{remote}z_{remote}+a_{env}z_{env}
$$

三个权重经过 softmax 归一化。这样，每个网格可以根据自身数据动态调整不同模态的贡献。

## 5. 3×3 邻域聚合

每个目标网格使用固定的 3×3 邻域。城市边界处不存在的邻居由显式掩码处理，中心网格始终有效。

邻域聚合由两条路径组成：

- **空间卷积路径**：将九个网格的融合表征恢复为 3×3 特征图，并拼接邻域有效性掩码，通过卷积提取局部空间格局。
- **多头注意力路径**：以中心网格为 query、九个邻域网格为 key 和 value，学习各邻居对中心网格的影响。

两条路径的结果经过 MLP 融合，再与中心表征进行残差连接和 LayerNorm：

$$
z_{neighbor}=\operatorname{LayerNorm}\left(z_{center}+f_{spatial,attention}\right)
$$

## 6. 回归输出

模型将中心网格表征和邻域增强表征拼接：

$$
z_{final}=[z_{center};z_{neighbor}]
$$

随后通过“Linear → ReLU → Dropout → Linear”回归头，输出网格的 `log1p` 碳排放预测。预测结果可还原到吨碳尺度：

$$
\widehat{\mathrm{emission}}_{tc}=\max(0,\exp(\hat{y})-1)
$$

## 7. 训练目标

主要回归损失为对数排放空间上的 MAE：

$$
\mathcal{L}_{reg}=\frac{1}{N}\sum_{i=1}^{N}|\hat{y}_i-y_i|
$$

同时使用 POI—遥感对比损失，使同一网格的两种表征相互接近：

$$
\mathcal{L}=\mathcal{L}_{reg}+\lambda\mathcal{L}_{contrastive}
$$

默认前两个 epoch 只优化回归损失，之后加入权重为 0.1 的对比损失。模型使用 AdamW 优化，以验证集 MAE 保存最佳 checkpoint，并通过 early stopping 控制训练轮数。

## 8. 核心思路

模型首先分别提取 POI、遥感和环境信息，通过模态注意力形成网格综合表示；随后结合中心网格周围的 3×3 空间上下文，最终预测月度碳排放。其核心是“多模态编码—自适应融合—局部空间建模—监督回归”，不涉及因果效应识别或环境不变性约束。
